name: Update WordPress Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  update-pages:
    runs-on: ubuntu-latest
    concurrency:
      group: update-pages
      cancel-in-progress: true
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run updater
        env:
          # Provide both legacy and standardized secret names for compatibility
          WP_USER: ${{ secrets.WP_USER }}
          WP_PASS: ${{ secrets.WP_PASS }}
          WP_USERNAME: ${{ secrets.WP_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_PASS }}
          WP_SITE_URL: ${{ secrets.WP_SITE_URL }}
        run: |
          python scripts/update_pages.py --content-dir content --media media_lookup.json
