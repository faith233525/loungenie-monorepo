name: Update WordPress Pages

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  update-pages:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate required secrets
        run: |
          if [ -z "${{ secrets.WP_SITE_URL }}" ]; then
            echo "ERROR: WP_SITE_URL is not set"
            exit 1
          fi
          if [ -z "${{ secrets.WP_USERNAME }}" ]; then
            echo "ERROR: WP_USERNAME is not set"
            exit 1
          fi
          if [ -z "${{ secrets.WP_APP_PASSWORD }}" ]; then
            echo "ERROR: WP_APP_PASSWORD is not set"
            exit 1
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run WordPress page updater
        env:
          WP_URL: ${{ secrets.WP_SITE_URL }}
          WP_USERNAME: ${{ secrets.WP_USERNAME }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
        run: node ai_premium_optimizer.js
